export default function VerifyPage() {
  return (
    <main className="mx-auto w-full max-w-lg flex-1 px-4 py-10">
      <h1 className="font-serif text-2xl text-[var(--color-ink)]">Verify me</h1>
      <p className="mt-2 text-sm text-[var(--color-neutral)]">
        Verification badges (LinkedIn identity, employer, verified local) unlock voting, tipping,
        asking, vouching, and offering arrival help. LinkedIn verification is pending OAuth app
        approval — email sign-in is the stand-in for now.
      </p>
    </main>
  );
}
